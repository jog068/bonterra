# No environment variables are required to run the app.
# Optional overrides:

# API server port (defaults to 3001; the Vite proxy in client/vite.config.ts
# must be updated to match if you change it)
# PORT=3001
